enum AppBarHeaders { home, about, skills, experience, projects, education, contact }

enum AppBarHeadersAxis { horizontal, vertical }

enum DeviceType {
  mobile,
  ipad,
  smallScreenLaptop,
  largeScreenDesktop,
  extraLargeTV
}
